# TODO 清单

## 产业链模块

- [ ] **搜索结果整合优化**：当前 `analyze_stream` 的"整合资料"阶段仅做了 `"\n\n".join()` 字符串拼接，缺少实质处理。待优化方向：
  - 跨关键词搜索结果去重（相同 URL / 相似内容）
  - 相关性排序，优先保留与产业链强相关的段落
  - 长文本摘要提取，避免超出 LLM context 时粗暴截断（当前硬截 15000 字符）
